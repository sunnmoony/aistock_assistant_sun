# A股智能助手 - 部署手册-完整版

> 本手册适用于 A股智能助手 项目，帮助您快速部署和使用系统

---

## 📋 目录

1. [系统要求](#1-系统要求)
2. [本地部署步骤](#2-本地部署步骤)
3. [Docker部署说明](#3-docker部署说明)
4. [GitHub Actions部署说明](#4-github-actions部署说明)
5. [配置说明](#5-配置说明)
6. [常见问题](#6-常见问题)

---

## 1. 系统要求

### 1.1 硬件要求

| 配置项 | 最低要求 | 推荐要求 |
|--------|---------|---------|
| CPU    | 双核处理器 | 四核处理器 |
| 内存   | 2GB      | 4GB+     |
| 硬盘   | 500MB 可用空间 | 1GB+ 可用空间 |
| 网络   | 需要互联网连接 | 稳定的宽带网络 |

### 1.2 软件要求

| 软件 | 最低版本 | 推荐版本 |
|------|---------|---------|
| Python | 3.8     | 3.10+   |
| 操作系统 | Windows 7 / Linux / macOS 10.14 | Windows 10/11 / Ubuntu 20.04+ / macOS 12+ |

### 1.3 网络要求

- 需要访问以下服务：
  - 股票数据源（AkShare、Pytdx、东方财富等）
  - AI API服务（Gemini、OpenAI兼容API等）
  - 推送服务（企业微信、飞书、Telegram、邮件等）

---

## 2. 本地部署步骤

### 2.1 方式一：一键安装（推荐Windows用户）

#### 步骤1：安装Python

1. 访问 https://www.python.org/downloads/
2. 下载 Python 3.10 或更高版本
3. 双击安装程序，**务必勾选 "Add Python to PATH"**
4. 点击 "Install Now" 完成安装

验证安装：
```cmd
python --version
```

#### 步骤2：安装项目依赖

双击运行 `install.bat` 文件，等待安装完成（约2-5分钟）。

#### 步骤3：启动程序

选择以下任一方式启动：

| 启动方式 | 脚本文件 | 说明 |
|---------|---------|------|
| 标准模式 | 一键启动-标准模式.bat | 完整功能，适合大多数电脑 |
| 轻量模式 | 一键启动-轻量模式.bat | 内存优化，适合低配置电脑 |
| Anaconda标准模式 | 一键启动-Anaconda标准模式.bat | 使用Anaconda环境的标准模式 |
| Anaconda轻量模式 | 一键启动-Anaconda轻量模式.bat | 使用Anaconda环境的轻量模式 |

---

### 2.2 方式二：手动安装（适合所有平台）

#### 步骤1：获取项目代码

```bash
# 如果使用Git
git clone <your-repo-url>
cd aistock_assistant_final

# 或者直接下载ZIP压缩包并解压
```

#### 步骤2：创建虚拟环境（推荐）

**Windows:**
```cmd
python -m venv venv
venv\Scripts\activate
```

**Linux/macOS:**
```bash
python3 -m venv venv
source venv/bin/activate
```

#### 步骤3：安装依赖

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

如果下载速度慢，可以使用国内镜像源：

```bash
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

#### 步骤4：配置环境变量

复制 `.env.example` 文件为 `.env`：

```bash
# Windows
copy .env.example .env

# Linux/macOS
cp .env.example .env
```

编辑 `.env` 文件，填入必要的配置信息（详见第5章配置说明）。

#### 步骤5：启动应用

**方式1：桌面模式（PyQt5）**
```bash
python main.py
```

**方式2：Web界面模式**
```bash
python main.py --webui
```

**方式3：仅Web服务模式**
```bash
python main.py --webui-only
```

**方式4：定时任务模式**
```bash
python main.py --schedule
```

访问 Web 界面：http://localhost:8000

---

## 3. Docker部署说明

### 3.1 方式一：使用 Dockerfile 构建

#### 步骤1：构建镜像

```bash
docker build -t aistock-assistant .
```

#### 步骤2：运行容器

```bash
docker run -d \
  -p 8000:8000 \
  --env-file .env \
  -v $(pwd)/data:/app/data \
  --name aistock-assistant \
  aistock-assistant
```

参数说明：
- `-d`: 后台运行
- `-p 8000:8000`: 端口映射
- `--env-file .env`: 加载环境变量文件
- `-v $(pwd)/data:/app/data`: 数据目录挂载
- `--name aistock-assistant`: 容器名称

---

### 3.2 方式二：使用 Docker Compose（推荐）

#### 步骤1：创建 docker-compose.yml

项目根目录已包含 `docker-compose.yml` 文件，内容如下：

```yaml
version: '3.8'

services:
  aistock:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./.env:/app/.env
    environment:
      - PYTHONUNBUFFERED=1
      - PYTHONDONTWRITEBYTECODE=1
    restart: unless-stopped
```

#### 步骤2：启动服务

```bash
docker-compose up -d
```

#### 步骤3：查看日志

```bash
docker-compose logs -f
```

#### 步骤4：停止服务

```bash
docker-compose down
```

---

### 3.3 Docker 常用命令

| 命令 | 说明 |
|------|------|
| `docker-compose ps` | 查看容器状态 |
| `docker-compose logs` | 查看日志 |
| `docker-compose restart` | 重启服务 |
| `docker-compose stop` | 停止服务 |
| `docker-compose start` | 启动服务 |

---

## 4. GitHub Actions部署说明

### 4.1 概述

GitHub Actions 是最简单的部署方式，零成本，无需服务器。系统会在每个工作日自动运行股票分析并推送通知。

---

### 4.2 部署步骤

#### 步骤1：Fork仓库

1. 访问项目的 GitHub 仓库
2. 点击右上角的 `Fork` 按钮
3. 等待 Fork 完成

#### 步骤2：配置 Secrets

1. 进入你的 Fork 仓库
2. 点击 `Settings` → `Secrets and variables` → `Actions`
3. 点击 `New repository secret` 添加以下 Secrets：

| Secret名称 | 说明 | 必填 | 示例 |
|------------|------|------|------|
| `GEMINI_API_KEY` | Google AI API Key | ✅（二选一） | `AIzaSy...` |
| `OPENAI_API_KEY` | OpenAI兼容API Key | ✅（二选一） | `sk-...` |
| `OPENAI_BASE_URL` | OpenAI兼容API地址 | 可选 | `https://api.deepseek.com/v1` |
| `OPENAI_MODEL` | 模型名称 | 可选 | `deepseek-chat` |
| `STOCK_LIST` | 自选股代码 | ✅ | `600519,000001,300260` |
| `WECHAT_WEBHOOK_URL` | 企业微信Webhook URL | 可选 | `https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=...` |
| `FEISHU_WEBHOOK_URL` | 飞书Webhook URL | 可选 | `https://open.feishu.cn/open-apis/bot/v2/hook/...` |
| `TELEGRAM_BOT_TOKEN` | Telegram Bot Token | 可选 | `123456789:ABC...` |
| `TELEGRAM_CHAT_ID` | Telegram Chat ID | 可选 | `123456789` |
| `EMAIL_SENDER` | 发件人邮箱 | 可选 | `your@email.com` |
| `EMAIL_PASSWORD` | 邮箱授权码 | 可选 | `your_password` |
| `EMAIL_RECEIVERS` | 收件人邮箱 | 可选 | `receiver@email.com` |

**重要提示：**
- `GEMINI_API_KEY` 和 `OPENAI_API_KEY` 至少配置一个
- `STOCK_LIST` 是必填项
- 其他配置项根据需要选择配置

#### 步骤3：启用 Actions

1. 进入仓库的 `Actions` 标签页
2. 找到 `A股智能分析 - 每日分析` 工作流
3. 点击 `I understand my workflows, go ahead and enable them`

#### 步骤4：手动触发测试

1. 进入 `Actions` 标签页
2. 点击 `A股智能分析 - 每日分析`
3. 点击右侧的 `Run workflow` 按钮
4. 填写参数：
   - `stock_list`: 自选股代码列表（逗号分隔）
   - `run_backtest`: 是否运行回测（默认false）
   - `notify`: 是否发送推送通知（默认true）
5. 点击绿色的 `Run workflow` 按钮开始执行

---

### 4.3 工作流说明

**自动触发时间：**
- 每个工作日（周一至周五）18:00（北京时间）自动执行

**手动触发：**
- 可以随时手动触发分析

---

## 5. 配置说明

### 5.1 环境变量配置（.env文件）

#### AI模型配置（至少配置一个）

```bash
# Gemini AI（免费）
GEMINI_API_KEY=your_gemini_api_key

# 或 OpenAI兼容API
OPENAI_API_KEY=your_openai_api_key
OPENAI_BASE_URL=https://api.deepseek.com/v1
OPENAI_MODEL=deepseek-chat
```

#### 自选股配置

```bash
STOCK_LIST=600519,000001,300260
```

#### 推送配置（可选）

```bash
# 企业微信
WECHAT_WEBHOOK_URL=https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=your_key

# 飞书
FEISHU_WEBHOOK_URL=https://open.feishu.cn/open-apis/bot/v2/hook/your_key

# Telegram
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_CHAT_ID=your_chat_id

# 邮件
EMAIL_SENDER=your@email.com
EMAIL_PASSWORD=your_password
EMAIL_RECEIVERS=receiver@email.com
```

---

### 5.2 配置文件（config.json）

系统支持通过 `config.json` 进行更详细的配置。

#### 数据源配置

```json
{
  "data_sources": {
    "akshare": {
      "enabled": true,
      "priority": 0
    },
    "pytdx": {
      "enabled": true,
      "priority": 1,
      "server_host": "111.229.247.189",
      "server_port": 7709,
      "max_retries": 3,
      "retry_delay": 2
    },
    "eastmoney": {
      "enabled": true,
      "priority": 2
    },
    "mock": {
      "enabled": true,
      "priority": 3
    }
  }
}
```

**数据源优先级说明：**
- 数字越小，优先级越高
- 系统会按优先级依次尝试获取数据
- 某个数据源失败时会自动降级到下一个

#### 推送配置

```json
{
  "notification": {
    "enabled": true,
    "channels": {
      "wechat": {
        "enabled": true,
        "webhook_url": "https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=xxx",
        "msg_type": "markdown"
      },
      "feishu": {
        "enabled": false,
        "webhook_url": "https://open.feishu.cn/open-apis/bot/v2/hook/xxx",
        "msg_type": "card"
      },
      "telegram": {
        "enabled": false,
        "bot_token": "xxx",
        "chat_id": "xxx"
      },
      "email": {
        "enabled": false,
        "sender": "your@email.com",
        "password": "xxx",
        "receivers": "receiver@email.com"
      }
    }
  }
}
```

#### 回测配置

```json
{
  "backtest": {
    "enabled": true,
    "eval_window_days": 10,
    "min_age_days": 14,
    "limit": 200
  }
}
```

---

### 5.3 命令行参数

```bash
# 查看帮助
python main.py --help

# 正常运行（PyQt5桌面模式）
python main.py

# Web界面模式
python main.py --webui

# 仅Web服务模式
python main.py --webui-only

# 定时任务模式
python main.py --schedule

# 运行回测
python main.py --backtest

# 指定股票分析
python main.py --stocks 600519,000001

# 禁用推送通知
python main.py --no-notify

# 单股推送模式（每分析完一只立即推送）
python main.py --single-notify
```

---

## 6. 常见问题

### 6.1 安装问题

**Q: 提示"Python未找到"**

**A:**
1. 重新安装Python，安装时务必勾选 "Add Python to PATH"
2. 或手动将Python添加到系统环境变量

**Q: pip安装依赖失败**

**A:**
```bash
# 更新pip
python -m pip install --upgrade pip

# 使用国内镜像源
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

---

### 6.2 运行问题

**Q: 程序启动后闪退**

**A:**
1. 检查 `logs/` 目录下的日志文件
2. 尝试使用轻量模式启动
3. 重新安装依赖

**Q: 无法获取股票数据**

**A:**
1. 检查网络连接
2. 尝试切换数据源
3. 检查是否有代理设置干扰

**Q: 内存占用过高**

**A:**
1. 使用轻量模式启动
2. 减少自选股数量
3. 增加刷新间隔

---

### 6.3 AI配置问题

**Q: 如何获取 Gemini API Key？**

**A:**
1. 访问 https://aistudio.google.com/
2. 创建新项目
3. 选择 "Get API Key"
4. 复制 API Key 到配置文件

**Q: 如何获取 OpenAI 兼容 API Key？**

**A:**
推荐使用以下服务商：
- DeepSeek：https://platform.deepseek.com/
- 通义千问：https://dashscope.aliyun.com/
- 硅基流动：https://cloud.siliconflow.cn/

注册账号后获取 API Key 和 Base URL 填入配置。

---

### 6.4 推送问题

**Q: 推送不工作怎么办？**

**A:**
1. 检查 Webhook URL 或 API Key 是否正确
2. 查看日志文件排查错误
3. 先用测试工具验证推送渠道是否正常

---

## 附录

### A. 支持的AI API提供商

| 提供商 | 官网 | 备注 |
|--------|------|------|
| Google Gemini | https://aistudio.google.com/ | 免费额度 |
| DeepSeek | https://platform.deepseek.com/ | 高性价比 |
| 通义千问 | https://dashscope.aliyun.com/ | 阿里云 |
| 硅基流动 | https://cloud.siliconflow.cn/ | 多模型支持 |

### B. 支持的推送渠道

| 渠道 | 类型 | 说明 |
|------|------|------|
| 企业微信 | Webhook | 适合团队使用 |
| 飞书 | Webhook | 适合团队使用 |
| Telegram | Bot | 适合个人使用 |
| 邮件 | SMTP | 通用推送方式 |

### C. 项目文件结构

```
aistock_assistant_final/
├── api/                    # API相关代码
├── bot/                    # 机器人相关代码
├── core/                   # 核心功能模块
│   ├── ai_agents/         # AI智能体
│   └── data_providers/    # 数据源
├── data/                   # 数据目录
├── docs/                   # 文档目录
├── logs/                   # 日志目录
├── models/                 # 数据模型
├── tests/                  # 测试代码
├── ui/                     # UI界面
├── .env                    # 环境变量配置
├── .env.example           # 环境变量示例
├── config.json            # 配置文件
├── Dockerfile             # Docker镜像文件
├── docker-compose.yml     # Docker Compose配置
├── requirements.txt       # Python依赖
├── main.py                # 主程序入口
└── 一键启动-*.bat         # Windows启动脚本
```

---

## 技术支持

如遇到问题，请按以下顺序排查：

1. 查看本文档的[常见问题](#6-常见问题)章节
2. 查看 `logs/` 目录下的日志文件
3. 查看项目 GitHub Issues
4. 联系技术支持

---

**祝您使用愉快！投资顺利！** 🎉
